apiVersion: apps/v1
kind: Deployment
metadata:
  name: mcp-registry-app
  namespace: mcp-gate
  labels:
    app: mcp-registry-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: mcp-registry-app
  template:
    metadata:
      labels:
        app: mcp-registry-app
    spec:
      containers:
      - name: mcp-registry-app
        image: ${HARBOR_HOST}/rnd-devops/images/mcp-registry:${BUILD_NUMBER}
        ports:
        - containerPort: 80
        env:
        - name: NODE_ENV
          valueFrom:
            configMapKeyRef:
              name: mcp-config
              key: NODE_ENV
        - name: PORT
          value: "80"
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 80
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 80
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: mcp-registry-service
  namespace: mcp-gate
spec:
  selector:
    app: mcp-registry-app
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
  type: ClusterIP 